{
  "name": "euler-v2",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "pnpm": "9.12.3"
  },
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "abis:sync": "./scripts/abisAddressesSync.sh",
    "verify:accountPositions": "bun ./verify/checkAccountPositions.ts",
    "verify:accountVaultBalances": "bun ./verify/checkAccountVaultBalances.ts",
    "test": "graph test",
    "tag:deployments": "bun scripts/tag-deployments.ts",
    "generate:deployments": "bun scripts/generate-deployments.ts",
    "prepare:mainnet": "bun scripts/prepare.ts mainnet",
    "prepare:base": "bun scripts/prepare.ts base",
    "prepare:arbitrum": "bun scripts/prepare.ts arbitrum",
    "prepare:swell": "bun scripts/prepare.ts swell",
    "prepare:sonic": "bun scripts/prepare.ts sonic",
    "prepare:ink": "bun scripts/prepare.ts ink",
    "prepare:berachain": "bun scripts/prepare.ts berachain",
    "prepare:bob": "bun scripts/prepare.ts bob",
    "prepare:unichain": "bun scripts/prepare.ts unichain",
    "prepare:avalanche": "bun scripts/prepare.ts avalanche",
    "prepare:bsc": "bun scripts/prepare.ts bsc",
    "prepare:worldchain": "bun scripts/prepare.ts worldchain",
    "prepare:hyperevm": "bun scripts/prepare.ts hyperevm",
    "prepare:optimism": "bun scripts/prepare.ts optimism",
    "prepare:gnosis": "bun scripts/prepare.ts gnosis",
    "prepare:tacturin": "bun scripts/prepare.ts tacturin",
    "prepare:tac": "bun scripts/prepare.ts tac",
    "build:mainnet": "pnpm prepare:mainnet && graph build",
    "build:base": "pnpm prepare:base && graph build",
    "build:arbitrum": "pnpm prepare:arbitrum && graph build",
    "build:swell": "pnpm prepare:swell && graph build",
    "build:sonic": "pnpm prepare:sonic && graph build",
    "build:ink": "pnpm prepare:ink && graph build",
    "build:berachain": "pnpm prepare:berachain && graph build",
    "build:berachain:thegraph": "pnpm prepare:berachain && graph build",
    "build:bob": "pnpm prepare:bob && graph build",
    "build:unichain": "pnpm prepare:unichain && graph build",
    "build:avalanche": "pnpm prepare:avalanche && graph build",
    "build:bsc": "pnpm prepare:bsc && graph build",
    "build:worldchain": "pnpm prepare:worldchain && graph build",
    "build:hyperevm": "pnpm prepare:hyperevm && graph build",
    "build:optimism": "pnpm prepare:optimism && graph build",
    "build:gnosis": "pnpm prepare:gnosis && graph build",
    "build:tacturin": "pnpm prepare:tacturin && graph build",
    "build:tac": "pnpm prepare:tac && graph build",
    "deploy:mainnet": "pnpm build:mainnet && bun scripts/deploy.ts mainnet && pnpm generate:deployments",
    "deploy:mainnet:test": "pnpm build:mainnet && bun scripts/deploy.ts mainnet test && pnpm generate:deployments",
    "deploy:mainnet:swap": "pnpm build:mainnet && bun scripts/deploy.ts mainnet swap && pnpm generate:deployments",
    "deploy:arbitrum": "pnpm build:arbitrum && bun scripts/deploy.ts arbitrum && pnpm generate:deployments",
    "deploy:base": "pnpm build:base && bun scripts/deploy.ts base && pnpm generate:deployments",
    "deploy:base:test": "pnpm build:base && bun scripts/deploy.ts base test && pnpm generate:deployments",
    "deploy:swell": "pnpm build:swell && bun scripts/deploy.ts swell && pnpm generate:deployments",
    "deploy:sonic": "pnpm build:sonic && bun scripts/deploy.ts sonic && pnpm generate:deployments",
    "deploy:ink": "pnpm build:ink && bun scripts/deploy.ts ink && pnpm generate:deployments",
    "deploy:berachain": "pnpm build:berachain && bun scripts/deploy.ts berachain && pnpm generate:deployments",
    "deploy:bob": "pnpm build:bob && bun scripts/deploy.ts bob && pnpm generate:deployments",
    "deploy:unichain": "pnpm build:unichain && bun scripts/deploy.ts unichain && pnpm generate:deployments",
    "deploy:avalanche": "pnpm build:avalanche && bun scripts/deploy.ts avalanche && pnpm generate:deployments",
    "deploy:bsc": "pnpm build:bsc && bun scripts/deploy.ts bsc && pnpm generate:deployments",
    "deploy:worldchain": "pnpm build:worldchain && bun scripts/deploy.ts worldchain && pnpm generate:deployments",
    "deploy:hyperevm": "pnpm build:hyperevm && bun scripts/deploy.ts hyperevm && pnpm generate:deployments",
    "deploy:optimism": "pnpm build:optimism && bun scripts/deploy.ts optimism && pnpm generate:deployments",
    "deploy:gnosis": "pnpm build:gnosis && bun scripts/deploy.ts gnosis && pnpm generate:deployments",
    "deploy:tacturin": "pnpm build:tacturin && bun scripts/deploy.ts tacturin && pnpm generate:deployments",
    "deploy:tac": "pnpm build:tac && bun scripts/deploy.ts tac && pnpm generate:deployments"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.87.0",
    "@graphprotocol/graph-ts": "0.33.0",
    "viem": "^2.22.22",
    "@types/node": "^22.13.10",
    "env-cmd": "^10.1.0",
    "matchstick-as": "0.6.0",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10"
  }
}